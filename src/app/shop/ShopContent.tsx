'use client'
import { useSearchParams } from 'next/navigation'

export default function ShopContent() {
  const searchParams = useSearchParams()
  
  // Your existing shop page logic here
  return (
    // Your existing shop page JSX
  )
} 